name: CI Pipeline

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: main
    steps:
      - name: Trigger Render Deploy
      - run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"